<%= form_with(model: author, local: true) do |form| %>
  <% if author.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(author.errors.count, "error") %> prohibited this author from being saved:</h2>

      <ul>
      <% author.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :author_name %>
  </div>

  <div class="field">
    <%= form.label :email %>
    <%= form.text_field :email, id: :author_email %>
  </div>

  <div class="field">
    <%= form.label :cell %>
    <%= form.text_field :cell, id: :author_cell %>
  </div>

  <div class="field">
    <%= form.label :address %>
    <%= form.text_area :address, id: :author_address %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
